spring.application.name=${CONFIG_HOST:config-server}
server.port=${CONFIG_PORT:8888}

eureka.client.service-url.defaultZone=http://${DISCOVERY_HOST:localhost}:${DISCOVERY_PORT:8761}/eureka

eureka.client.fetch-registry=false

spring.cloud.config.server.native.search-locations=classpath:/
spring.profiles.active=native
