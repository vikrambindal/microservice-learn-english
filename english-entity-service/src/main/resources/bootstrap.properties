eureka.client.service-url.defaultZone=http://${DISCOVERY_HOST:localhost}:${DISCOVERY_PORT:8761}/eureka
spring.cloud.config.uri=http://${CONFIG_SERVER_HOST:localhost}:${CONFIG_SERVER_PORT:8888}
spring.cloud.config.discovery.serviceId=${CONFIG_SERVER_HOST:config-server}
spring.cloud.config.discovery.enabled=true
server.port=${SERVER_PORT:0}
